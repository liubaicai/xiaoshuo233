<h4><%= @book.title %></h4>
<div class="row">
  <% @book.catalogs(:order => [ :catalog_id ]).each do |catalog| %>
      <div class="col-sm-3"><a href='/<%= @book.id %>/<%= catalog.catalog_id %>.html'><%= catalog.title %></a></div>
  <% end %>
</div>
