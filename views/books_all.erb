<h4><%= @page_title || 'Books' %></h4>
<div class="row">
  <% @books.each do |book| %>
      <div class="col-sm-3"><a href='/<%= book.id %>.html'><%= book.title %></a></div>
  <% end %>
</div>
<div>
  <%= will_paginate @books, renderer: BootstrapPagination::Sinatra %>
</div>